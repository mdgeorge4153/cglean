\newtheorem{definition}{Definition}
\newtheorem{theorem}{Theorem}
\newtheorem{lemma}{Lemma}
\newtheorem{corollary}{Corollary}
\newtheorem{proposition}{Proposition}

\chapter{Overview}

\chapter{2D Planar Geometry Primitives}

\chapter{Planar Arrangements}
\label{chap:arrangements}

\section{What it means for a geometric algorithm to be correct}

Before describing arrangements themselves, it is worth being explicit about the
shape of the correctness statements in this chapter, because there is a real
choice to be made and it determines how the rest of the development is
organised.

A geometric algorithm here has two audiences. It must \emph{run} --- an
arrangement is a finite data structure, and computing the union of two of them
is a program that terminates and produces another arrangement. It must also be
\emph{right}, and ``right'' has to be measured against something that is not
itself a program, or we have only shown that our code agrees with our code.

\subsection{Two levels of specification}

There are two distinct things we could measure an implementation against, and
they are not equally strong.

The first is an \emph{internal} specification, phrased in terms of the
operations this library provides. The \code{Region} interface exposes a
\code{location} function assigning each point one of \emph{inside},
\emph{boundary} or \emph{outside}, and we can say how union must behave on those
three values --- this is \code{union\_spec}, and \code{LawfulRegion} demands that
\code{location} of a union agrees with it pointwise. This is a genuine
specification: it constrains observable behaviour rather than internal
structure, so two different representations of a region can be held to the same
standard, and an implementation cannot satisfy it by accident.

It has one weakness. If \code{union\_spec} is subtly wrong --- if it mishandles
the boundary case, say --- then every implementation we verify against it is
wrong in exactly the same way, consistently and provably.

The second is an \emph{external} specification, phrased in terms Mathlib already
defines. A region determines a set of points, and \code{Region.interior} already
exposes it as a \code{Set}. At that level, union really is $\cup$, Minkowski sum
really is pointwise addition of sets, and the convex hull really is Mathlib's
\code{convexHull}. A theorem stating that our computed hull has the same
interior as Mathlib's \code{convexHull} says that ours is \emph{the} convex hull,
not that ours satisfies our definition of one.

Mathlib's \code{convexHull} is defined as the intersection of all convex sets
containing its argument. It cannot be executed, and that is precisely why it
makes a good referee: it is a definition nobody would mistake for an algorithm,
agreed on by a large community, and it cannot have been tailored to make our
implementation look correct.

The two levels are complementary rather than competing. The internal
specification is what one actually reasons with when proving a sweep line
correct, since it is stated in terms the algorithm manipulates. The external
one is what makes the result mean something. The intended structure is therefore
to prove the internal specification directly, and separately to prove that the
internal specification implies the external one --- so that each algorithm
proof is tractable, while the top-level claim is stated in Mathlib's language.

\subsection{Where the algorithm lives}

Given a specification, there are several ways to relate it to code, and the
choice is not obvious.

One option is to make the implementation carry its own correctness, returning a
value bundled with a proof that it satisfies the specification. Nothing
incorrect can then escape. This works well for small functions, but for a sweep
line it fuses the algorithm and its proof into a single artifact, and every
change to the algorithm disturbs the proof.

A second option is to define the operation twice: once in whatever form is
easiest to reason about, and once in the form we actually want to run, together
with a theorem that the two agree. Lean supports this directly --- a
\code{csimp} lemma causes the compiler to substitute the efficient version at
runtime while every proof continues to refer to the clean one. This is how the
Lean standard library treats its own primitives. The cost is that proving two
algorithms extensionally equal can be harder than proving one of them correct,
particularly when they have quite different shapes, as a naive quadratic union
and a sweep line do.

A third option applies specifically to decision problems. Point location asks a
question with a yes-or-no answer, so instead of writing a function and proving
it correct, we can state the question as a proposition and supply a decision
procedure for it. The algorithm is then the proof, and no separate agreement
theorem is required at all. This is available for \code{location} but not for
union or convex hull, which produce data rather than answers.

These options are not exclusive, and a fourth concern cuts across all of them:
how much of an arrangement's validity is enforced by its type. The invariants
listed with the \code{Arrangement} definition --- that edges point
lexicographically downwards, that every target is a vertex of the map, that
edges around a vertex are sorted, and that edges and vertices are
interior-disjoint --- could be carried in the structure, making malformed
arrangements unrepresentable, or stated as a separate predicate that operations
are proved to preserve. The former makes every constructor prove more; the
latter makes every consumer assume more.

\chapter{Point Location Queries}

\chapter{Convex Hull}

\chapter{Minkowski Sum}

\begin{definition}
  \label{def:minkowski}
  Given sets of points $A$ and $B$, the Minkowski sum of $A$ and $B$, written
  $A ⊕ B$ is the set $\{a + b \mid a ∈ A, b ∈ B\}$.
\end{definition}

\begin{theorem}
  \label{thm:minkowski_convex_poly}
  \uses{def:minkowski}
  \notready
  If $A$ and $B$ are convex polygons, then you can compute the minkowski sum by
  taking the convex hull of the sums of the corners.
\end{theorem}

\begin{theorem}
  \label{thm:minkowski_union}
  \uses{def:minkowski}
  \notready
  The Minkowski sum of the union is the union of the sums
\end{theorem}

\chapter{Computational Number Representations}
\label{chap:numbers}

For our computational geometry algorithms to be correct, we need to use exact
number representations --- roundoff error introduced by floating-point
approximations can easily lead to incorrect outcomes.  For example,
if the intersection of two polygons is a single point or line, small rounding
errors could easily make the intersection look empty.

Mathlib provides an exact real number representation that encodes a number as
an infinite sequence of rational approximations. Unfortunately, this
representation is not suitable for computation because some operations are not
computable. For example, determining whether two reals are equal requires
iterating over the entire infinite sequences that represent them.

The rational numbers ($ℚ$) are a reasonable choice of number type: fractions are easy
to compute with and don't introduce any rounding error. Some applications
require irrational numbers though. For example, rotating a unit line segment by
$45°$ produces a point with coordinates $(\sqrt{2}/2, \sqrt{2}/2)$, whose
coordinates are irrational.

If we only cared about $45°$ rotations, we could store numbers of the
form $a_1 + a_2\sqrt{2}$ where $a_1$ and $a_2$ are rational numbers. It
turns out that adding, multiplying, and dividing numbers of this form produces
numbers of this form, and it is not too hard to compare them for equality and
inequalities; these operations are sufficient to perform many geometric
operations (including polygon union and intersection).

Of course if you give a mouse a $45°$ rotation, they will want a
rotation of $30°$. We can support both $45°$ and $30°$
rotations with numbers of the form
$a_1 + a_2\sqrt{2} + a_3\sqrt{3} + a_6\sqrt{6}$; these numbers also support the
important operations for many geometric algorithms.

This suggests that the choice of number representation should be application
specific, and the geometric algorithms should be compatible with different
number representations. This package constructs a few concrete number
representations that applications can use, and proves that they are correct.

In this context, correctness means that the numbers support the necessary
operations, and that those operations behave as you would expect. More
concretely, we show that the number representations form ``linearly ordered
fields,'' which are types that support addition, multiplication, subtraction,
division, and computable comparisons (equality and inequality), all of which
must satisfy certain axioms.

The first implementation is \code{AdjoinSqrt A n} (abbreviated $A[\sqrt{n}]$),
which represents numbers of the form $a_1 + a_n\sqrt{n}$. Instead of requiring
$a_1$ and $a_n$ to be rationals, we allow them to be taken from any fixed
linearly ordered field $A$.  This allows the construction to be repeated. For
example, we can factor numbers of the form $a_1 + a_2\sqrt{2} + a_3\sqrt{3} +
a_6\sqrt{6}$ as $(a_1 + a_2\sqrt{2}) + (a_3 + a_6\sqrt{2})\sqrt{3}$, and then
represent them using $ℚ[\sqrt{2}][\sqrt{3}]$.

The second implementation is intended to improve performance by using
floating-point approximations wherever possible, and only falling back on the
exact computations when the floating-point arithmetic is not precise enough. We
define \code{FilteredReal α} which wraps deferred values of $α$
inside a floating-point interval.

We can summarize the example from this section in the following corollary:
\begin{corollary}
  \label{thm:numberType}
  \uses{def:adjoin, def:filtered}
  \code{FilteredReal $ℚ[\sqrt{2}][\sqrt{3}]$} is a linearly ordered field.
\end{corollary}

\begin{proof}
  \uses{thm:adjoinOrderedField, thm:filteredOrderedField, thm:adjoinToRealOrderedFieldHom}
  TODO
\end{proof}

The definition of a linearly ordered field can be broken down into parts, and
these parts are important stepping stones on the path to proving that a number
representation is a linearly ordered field. In particular, we will mention the
following intermediate interfaces:

\begin{enumerate}
\item A (commutative) ring must support addition, subtraction, and
multiplication, but not division. For example, the integers ($ℤ$) are a ring
but not a field. Rings are useful in this context because one can generate a
field from a ring by forming fractions; an alternative approach to defining
$ℚ[\sqrt{2}][\sqrt{3}]$ would be to take the field of fractions of
$ℤ[\sqrt{2}][\sqrt{3}]$.

\item A field is a ring that also supports division.

\item A linear order is a type that supports comparisons ($≤$, $<$, etc).

\item A linearly ordered ring is a ring with a linear order that is compatible
with the ring operations; a linearly ordered field is a field with a linear
order that is compatible with the field operations.
\end{enumerate}

Note that we are borrowing all of these definitions from Mathlib, so we are
leaving the exact definitions a bit vague here.  These definitions can also be
broken down further (e.g. Monoid, Group, Partial Order, etc) and we do prove
related intermediate results where possible, but these are probably less useful
for this library so we don't describe them here.

\section{Adjoining a square root}

%% Definition

\begin{definition}
  \label{def:adjoin}
  \lean{AdjoinSqrt}
  \leanok
  $A[\sqrt{n}]$ is stored as a pair $(a_1, a_n)$, representing the value
  $a_1 + a_n\sqrt{n}$.
\end{definition}

We begin by checking that $A[\sqrt{n}]$ is a ring. We will then proceed to check
that it is a field, and that it has a natural ordering on it ($<$ and $≤$).

\begin{definition}
  \label{def:adjoinRingOps}
  \uses{def:adjoin}
  \lean{AdjoinSqrt.ringOps}
  \leanok
  We define the basic ring operations of addition, negation, and multiplication
  in the usual way.
  For example, $(a_1 + a_n\sqrt{n})(b_1 + b_n\sqrt{n}) = (a_1b_1 + na_nb_n) + (a_1b_n + a_nb_1)\sqrt{n}$.

  Note that this definition only makes sense if $A$ supports these operations.
\end{definition}

%% A[√n] is a ring

\begin{proposition}
  \label{thm:adjoinRing}
  \lean{AdjoinSqrt.instCommRing}
  \leanok
  \uses{def:adjoinRingOps}
  If $A$ satisfies the ring properties, then so does $A[\sqrt{n}]$.
\end{proposition}

\begin{proof}
  \leanok
  Follows immediately from the definitions.
\end{proof}

%% A[√n] has √n

Of course, the whole point of $A[√n]$ is that it contains a square root of $n$:
\begin{theorem}
  \label{thm:adjoinHasSqrtN}
  \lean{AdjoinSqrt.root_n_squared}
  \uses{def:adjoin, thm:adjoinRing}
  \leanok
  If $A$ is a ring, then in $A[\sqrt{n}]$, $\sqrt{n} * \sqrt{n} = n$
\end{theorem}

\begin{proof}
  \leanok
  Follows immediately from the definitions.
\end{proof}

%% A[√n] is a field

In order to check that $A[\sqrt{n}]$ is a field, we need to define division. In
fact, we only need to define $x^{-1}$ for $x ∈ A[\sqrt{n}]$, which we do as
follows:

\begin{definition}
  \label{def:adjoinInv}
  \lean{AdjoinSqrt.instInv}
  \leanok
  \uses{def:adjoinRingOps}
  We define $(a_1 + a_n\sqrt{n})^{-1}$ as follows:
  \begin{eqnarray*}
    \frac{1}{a_1 + a_n\sqrt{n}}
      & = & \frac{1}{a_1 + a_n\sqrt{n}} \cdot \frac{a_1 - a_n\sqrt{n}}{a_1 - a_n\sqrt{n}}
        =   \frac{a_1 - a_n\sqrt{n}}{a_1^2 - na_n^2} \\ \\
      & = & a_1(a_1^2 - na_n^2)^{-1} - a_n(a_1^2 - na_n^2)^{-1}\sqrt{n} \\
  \end{eqnarray*}
  As long as $A$ has inverses, this is an element of $A[\sqrt{n}]$.
\end{definition}

\begin{lemma}
  \label{thm:adjoinField}
  \lean{AdjoinSqrt.instField}
  \leanok
  \uses{def:adjoin, thm:adjoinRing, def:adjoinInv}
  If $A$ is a field and $n$ is not a square in $A$, then $A[\sqrt{n}]$ is a
  field.
\end{lemma}

\begin{proof}
  \leanok
  This basically boils down to the computation given in the definition above.

  However, we need the extra condition that $n$ is not a square because
  otherwise we could define the same number in two different ways. For example,
  if $n$ was $4$, we would have $0 + 1\sqrt{4} = 2 + 0\sqrt{4}$. This would
  cause a technical problem with the proof.
\end{proof}

%% A[√n] is ordered ring

Now we can proceed to check the ordering properties on $A[\sqrt{n}]$. Instead
of defining comparisons like $<$ and $≤$ directly, it is easier to define a
$sign$ operator that indicates whether a number is greater than, equal to,
or less than 0 (this reduces the case analysis considerably).

\begin{definition}
  \label{def:signedRing}
  \lean{SignedRing}
  \leanok
  A signed ring is a ring $A$ coupled with a function $sign : A → \{0, 1, -1\}$
  satisfying the following properties: \begin{itemize}
    \item $sign~0 = 0$
    \item $sign~1 = 1$
    \item $sign~(ab) = (sign~a)(sign~b)$
    \item if $sign~a = 0$ then $a = 0$
    \item $sign~(-a) = -sign~a$
    \item if $sign~a \geq 0$ and $sign~b~\geq 0$ then $sign~(a + b) \geq 0$
  \end{itemize}
\end{definition}

\begin{lemma}
  \label{thm:orderFromSign}
  \uses{def:signedRing}
  \lean{instLinearOrderOfSignedRing, instIsStrictOrderedRingOfSignedRing}
  \leanok
  Given a signed ring $A$, we can define an ordered ring structure on $A$.
\end{lemma}
\begin{proof}
  \leanok
  This is mostly borrowed from Mathlib.
\end{proof}
\begin{lemma}
  \label{thm:signFromOrder}
  \uses{def:signedRing}
  \lean{instSignedRingOfLinearOrderedCommRing}
  \leanok
  Conversely, given an ordered ring $A$ we can define a well-behaved $sign$
  function.
\end{lemma}
\begin{proof}
  \leanok
  This follows immediately from the definitions.
\end{proof}

%% Ordering on A[√n]

With this tool in hand, we can define an ordering on $A[\sqrt{n}]$:

\begin{definition}
  \label{def:adjoinSign}
  \lean{AdjoinSqrt.instSignedRing}
  \leanok
  \uses{def:signedRing, def:adjoin, def:adjoinRingOps}
  If $A$ is a signed ring and $n ∈ A \geq 0$ is not a square in $A$, then
  $A[\sqrt{n}]$ is a signed ring.
\end{definition}

\begin{proof}
  We want to define $sign : A → \{0,1,-1\}$ which compares $a_1 + a_n\sqrt{n}$
  with 0. This is the same as comparing $a_1$ with $-a_n\sqrt{n}$. We would
  like to square both sides, but there's some casework involved, because
  depending on the signs of $a_1$ and $-a_n$, we may have to flip the
  inequality.

  If exactly one of $a_1$ and $-a_n$ are negative, the comparison is easy. If they
  are both positive, then comparing $a_1^2$ and $na_n^2$ gives the right
  answer. If they are both negative, then comparing $a_1^2$ and $na_n^2$ gives
  the opposite answer (this arithetic depends on $n$ being nonnegative).

  We can use this algorithm to define the sign function for $A[\sqrt{n}]$.

  Checking the signed ring properties is not entirely routine. Multiplicativity
  of the sign reduces to closure of the non-negative elements under
  multiplication, where the norms compose. Closure under addition is harder,
  since they do not; the argument there compares $(v-a)(v+a)$ with $(d-u)(d+u)$
  and multiplies through by $d+u$ to stay inside $A$.
  \leanok
\end{proof}

\begin{corollary}
  \label{thm:adjoinOrderedRing}
  \lean{AdjoinSqrt.linearOrderOfNonsquareOfPos, AdjoinSqrt.isStrictOrderedRingOfNonsquareOfPos}
  \uses{thm:adjoinRing, def:adjoinSign, thm:orderFromSign, thm:signFromOrder}
  \leanok
  If $A$ is a linearly ordered ring, and if $n$ is a positive non-square in $A$
  then $A[\sqrt{n}]$ is also linearly ordered.
\end{corollary}

\begin{proof}
  \leanok
  Since $A$ is a linearly ordered ring, it is a signed ring. Therefore $A[\sqrt{n}]$ is signed,
  which implies that it is a linearly ordered ring.
\end{proof}

%% A[√n] is an ordered field

Putting this all together gives the main result of this section:

\begin{theorem}
  \label{thm:adjoinOrderedField}
  \lean{AdjoinSqrt.instField, AdjoinSqrt.linearOrderOfNonsquareOfPos, AdjoinSqrt.isStrictOrderedRingOfNonsquareOfPos}
  \uses{def:adjoin, thm:adjoinField, thm:adjoinOrderedRing}
  \leanok

  If $A$ is a linearly ordered field, and if $n$ is a positive non-square in $A$
  then $A[\sqrt{n}]$ is also a linearly ordered field.
\end{theorem}

\begin{proof}
  \leanok
  The field structure is Lemma~\ref{thm:adjoinField} and the order is
  Corollary~\ref{thm:adjoinOrderedRing}; since both are instances on the same
  type, nothing further is needed to combine them.
\end{proof}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\subsection{Converting from $A[\sqrt{n}]$ to $ℝ$}

In order to use $A[\sqrt{n}]$ with \code{FilteredReal}, we need to have a way of
converting values in $A[\sqrt{n}]$ to real numbers, and this conversion has to preserve
the operations defined on $A[\sqrt{n}]$.

TODO: informal definitions for homs.

%% A[√n] → ℝ

\begin{definition}
  \label{def:adjoinToReal}
  \lean{AdjoinSqrt.toReal}
  \leanok
  \uses{def:adjoin}
  Given a function $toReal : A → ℝ$ and $n ∈ A$ with $n.toReal \geq 0$,
  there is a corresponding function $AdjoinSqrt.toReal : A[√n] → ℝ$ given by
  $(a_1 + a_n\sqrt{n}).toReal := a_1.toReal + a_n.toReal * \sqrt{n.toReal}$.
\end{definition}

\begin{lemma}
  \label{thm:adjoinToRealRingHom}
  \lean{AdjoinSqrt.toReal_zero, AdjoinSqrt.toReal_one, AdjoinSqrt.toReal_add, AdjoinSqrt.toReal_mul}
  \leanok
  \uses{def:adjoinToReal, thm:adjoinRing}
  If $A$ is a ring with a non-square $n ∈ A$ and $toReal : A → ℝ$ is a
  ring homomorphism, then $AdjoinSqrt.toReal : A[\sqrt{n}] → ℝ$ is a ring
  homomorphism.
\end{lemma}

\begin{lemma}
  \label{thm:adjoinToRealOrderHom}
  \lean{AdjoinSqrt.toReal_monotone}
  \leanok
  \uses{def:adjoinToReal, thm:adjoinOrderedRing}
  If $A$ is an ordered ring with a positive non-square $n ∈ A$ and
  $toReal : A → ℝ$ is monotonic then $AdjoinSqrt.toReal : A[\sqrt{n}] → ℝ$ is
  also monotonic.
\end{lemma}


\begin{lemma}
  \label{thm:adjoinToRealOrderedFieldHom}
  \uses{def:adjoinToReal, thm:adjoinOrderedField, thm:adjoinToRealRingHom, thm:adjoinToRealOrderHom}
  If $A$ is an ordered field with a positive non-square $n ∈ A$ and $toReal : A
  → ℝ$ is an ordered field homomorphism then $AdjoinSqrt.toReal : A[\sqrt{n}] →
  ℝ$ is also an ordered field homomorphism.
\end{lemma}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\subsection{Converting from $A$ to $A[√n]$}

These are some interesting results that aren't directly needed to use these
number representations, but are interesting nonetheless.

%% A → A[√n]

\begin{definition}
  \label{def:adjoinOfA}
  \lean{AdjoinSqrt.instCoe}
  \leanok
  \uses{def:adjoin}
  We write $A \hookrightarrow A[\sqrt{n}]$ for the natural embedding
  $a \mapsto a + 0\sqrt{n}$ (assuming $0 ∈ A$).
\end{definition}

%% A[√n] is an A-algebra

\begin{lemma}
  \label{thm:adjoinAlgebra}
  \lean{AdjoinSqrt.instAlgebra}
  \leanok
  \uses{def:adjoin, def:adjoinOfA, thm:adjoinRing}
  If $A$ is a ring, then $A[\sqrt{n}]$ is an $A$-algebra.
\end{lemma}

\begin{proof}
  Trivial
  \leanok
\end{proof}

\begin{lemma}
  \label{thm:mathlibAdjoin}
  \uses{def:adjoin}
  $A[\sqrt{n}]$ is equivalent to $A[x] / (x^2 - n)$ as defined in mathlib.
\end{lemma}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Interval arithmetic}

\begin{definition}
  \label{def:filtered}
  \notready
  \lean{FilteredReal}
  Given a function $f : α → ℝ$, a \code{FilteredReal f} value consists of
  an unevaluated value of type $α$ and a closed floating-point interval that
  contains $f α$. We consider two values in \code{FilteredReal f} to be
  equal if the wrapped $α$ values are equal; the intervals are just an
  approximation.
\end{definition}

\begin{definition}
  \label{def:filteredRingOps}
  \uses{def:filtered}
  \lean{FilteredReal.instRingOps}
  We define the usual ring operations on \code{FilteredReal} values by storing
  thunks that perform the corresponding operation on the underlying $α$ value.
  We also immediately compute a new interval using the corresponding operations
  on the endpoints of the intervals, taking care to round in the correct
  direction.

  In order for this definition to work, $f$ must be a ring homomorphism. This is
  because the $FilteredReal$ type requires a proof that the exact $α$ value lies
  inside the floating-point interval. Therefore to define addition, for example,
  we need to prove that the sum of the $α$ values lies inside the sum of the
  intervals, which we can only do if $f$ preserves sums.
\end{definition}

\begin{lemma}
  \label{thm:filteredRing}
  \lean{FilteredReal.instCommRing}
  \notready
  \uses{def:filteredRingOps}
  If $A$ is a ring and $f : A → ℝ$ is a ring homomorphism then \code{FilteredReal f}
  is a ring.
\end{lemma}

\begin{proof}
  \notready
\end{proof}

\begin{definition}
  \label{def:filteredInv}
  \lean{FilteredReal.instInv}
  \uses{def:filtered}
  If $A$ is a field, and $f$ is a field homomorphism, we can also define
  division in the same way.
\end{definition}

% TODO (prose): the filter's escape hatch. Division and overflow are the same
% event, so this should be one discussion rather than two.
%
%  * An interval containing zero has no reciprocal that is again an interval.
%    Two shapes of answer: a value meaning "no information", or a type of
%    intervals known to exclude zero. LeanCert takes the second
%    (`IntervalRatNonzero`), and offers reciprocals only for rational intervals,
%    not the dyadic ones used here.
%
%  * The subtype is the better fit, and not only on taste. A no-information
%    value propagates silently; requiring the divisor to be bounded away from
%    zero forces the question at the point of division, and the answer there is
%    what a filter should do anyway --- stop trusting the approximation and
%    evaluate.
%
%  * Doing that needs something the current interface lacks. To bracket a
%    freshly evaluated value one must approximate an element of $A$, and
%    $f : A \to \R$ does not do that: it names the real, it does not bound it.
%    So `FilteredEmbedding` needs either a separate approximation function, in
%    the manner of `Floatable`, or an embedding yielding intervals.
%
%  * Rounding belongs in the number layer, not the interval layer. LeanCert puts
%    it in the interval (`roundOut` takes a minimum exponent), which is
%    meaningful for dyadics and meaningless for floats. A class of numbers with
%    directed rounding, with the interval layer merely choosing a direction per
%    endpoint, is what survives swapping the representation.
%
%  * That class needs only directedness --- `addDown x y <= x + y <= addUp x y`
%    in $\R$ --- not correct rounding. IEEE implies it comfortably.
%
%  * For hardware floats the obstacle is that Lean's `Float` is opaque with no
%    theorems, so it has no real-valued semantics to reason from. One would
%    define `toReal` and assert a relative-error bound against it, in the manner
%    of Flocq for Coq, and derive the directed operations rather than asserting
%    them.
%
%  * NaN needs no embedding in $\R$: condition each specification on the
%    *output* being finite, and it is never mentioned. Where the condition
%    fails, the interval carries no information --- the same escape hatch as
%    division above.
%
%  * Consequence: the interval type needs a no-information case exactly when the
%    number type can overflow. Vacuous for dyadics, whose exponent is unbounded.
%
%  * Mathlib's `Mathlib/Data/FP/Basic.lean` is not a shortcut. Its `FP.Float` is
%    a separate inductive modelling inf, nan and finite values, unconnected to
%    the hardware `Float`, carrying one theorem. Its `toRat` does take a
%    finiteness proof, which is the same instinct as the above.

\subsection{Converting between $A$ and \code{FilteredReal (f : A → ℝ)}}

The whole point of \code{FilteredReal} is that it encapsulates values of
$A$.

\begin{definition}
  \label{def:filteredToA}
  \uses{def:filtered}
  \notready
  There is a nice injection $i$ from \code{FilteredReal (f : A → ℝ)} to
  $A$, and $f \circ i = toReal$.
\end{definition}

\begin{theorem}
  \label{def:filteredEquivalent}
  \uses{def:filteredToA, thm:filteredOrderedField}
  \notready
  $i$ preserves all of the linearly ordered field operations.
\end{theorem}

\subsection{FilteredReal avoids computation}

There's no way to prove in lean that the underlying $A$ values are not computed
unless strictly necessary, but we can at least prove that the interval
comparison only fails if the comparison can't be determined just from the
intervals. We formalize this as follows:

\begin{lemma}
  \label{thm:compareComplete}
  \uses{def:intervalCompare}
  \lean{CGLean.compare?_complete}
  \leanok
  If the function we use to compare intervals indicates that they are
  incomparable, then we can choose two sets of points in the two intervals that
  would give different comparison results.
\end{lemma}

\begin{proof}
  \leanok
  Declining means the intervals overlap, so the larger of the two lower bounds
  lies in both. That point compares equal to itself, while a suitable pair of
  endpoints compares strictly; the two pairs are the required witnesses.
\end{proof}

\chapter{Applications}

\section{Collision-Avoiding Drag and Drop}

\section{Origami}

\section{Diagram Widgets}

\chapter{Future Work}

\section{A structural account of $A[\sqrt{n}]$}

The development of $A[\sqrt{n}]$ in this document is deliberately elementary:
elements are pairs, and the ring and field laws are checked coordinatewise. That
is easy to follow and easy to compute with, but it obscures a good deal of
structure, and the ring proofs feel more laborious than the mathematics
warrants.

What the construction really is, is the \emph{regular representation}. Every
element $z$ of $A[\sqrt{n}]$ acts on $A[\sqrt{n}]$ by multiplication, and that
action is $A$-linear, so in the basis $\{1, \sqrt{n}\}$ it is a matrix over $A$:
\begin{displaymath}
  M(z) \;=\;
  \begin{pmatrix} z_1 & n z_2 \\ z_2 & z_1 \end{pmatrix},
  \qquad z = z_1 + z_2\sqrt{n}.
\end{displaymath}
The second column is where $n$ enters, and it is the only place. Since
$(zw)x = z(wx)$, the map $z \mapsto M(z)$ is a ring homomorphism, and
multiplying the matrices out reproduces the multiplication formula this
development defines by hand. Under this correspondence:
\begin{itemize}
\item the norm $z \cdot \overline{z}$ is $\det M(z)$, so its multiplicativity is
      that of the determinant;
\item $2z_1$ is $\operatorname{tr} M(z)$;
\item conjugation is the adjugate, so the formula for the inverse is Cramer's
      rule, and the fact that the norm vanishes only at zero is the statement
      that a matrix is invertible exactly when its determinant is not;
\item the characteristic polynomial is
      $\lambda^2 - 2z_1\lambda + (z_1^2 - n z_2^2)$, whose coefficients lie in
      $A$ while its roots are $z_1 \pm z_2\sqrt{n}$ --- the two conjugate
      embeddings.
\end{itemize}

That last point explains why the ordering can be described at all. The order
asks whether one particular eigenvalue is non-negative, and $\sqrt{n}$ appears
in the roots but never in the coefficients; the criterion of
Definition~\ref{def:adjoinSign} is the standard way of locating a root of a
quadratic from its coefficients. It also explains the shape of the argument for
closure under addition, which pairs $(v-a)(v+a)$ against $(d-u)(d+u)$: those are
the elementary symmetric combinations through which roots and coefficients
communicate.

This suggests presenting $A[\sqrt{n}]$ as $A[x]/(x^2 - n)$ and inheriting its
structure rather than verifying it. Mathlib's \code{AdjoinRoot} supplies the
commutative ring, the algebra structure, decidable equality, a power basis, and
--- given irreducibility, which is what the non-square hypothesis amounts to ---
the field structure. That would remove essentially all of the ring boilerplate
here, including the one proof in this development that has to navigate
associativity and commutativity by hand. Mathlib also defines \code{Algebra.norm}
as precisely the determinant of the multiplication map, so the norm and its
multiplicativity would come from the same place.

What it would \emph{not} simplify is the ordering, which is the bulk of the work
and was the difficult part. Mathlib supplies the ring and field theory of an
extension but no order theory for one: that is Artin--Schreier territory, and
the results are not there. The matrix picture explains what the sign function is
doing without providing a theorem one can cite in its place.

So the honest assessment is that the refactor is worth doing for the algebra and
irrelevant to the analysis. There is a second reason to want it, though, which
has nothing to do with brevity: the elementary presentation is a completely
concrete instance of a construction usually met in the abstract, and the two
side by side --- pairs of numbers on one hand, a quotient of a polynomial ring
on the other, provably the same --- would make a good introduction to the
commutative algebra involved.

One caveat for whoever attempts it: \code{AdjoinRoot} is a quotient type and its
field structure is noncomputable, so it would serve for reasoning while the
existing pair representation continued to serve for computation, the two related
by a theorem. That is the same division discussed in
Chapter~\ref{chap:arrangements}, and it recurs often enough in this project to
be worth naming as a habit rather than rediscovering each time.

\section{Deferred components}

Interval arithmetic, and with it the \code{FilteredReal} layer of
Chapter~\ref{chap:numbers}, is not currently built. It depends on an external
floating-point interval library which lags the Mathlib version used here.

There is a difficulty of principle behind that, not merely of packaging. Sound
interval arithmetic requires reasoning about floating-point, and Lean's
\code{Float} is an opaque primitive with no specification --- fast, but nothing
can be proved about it. A library that wants proofs must therefore supply its own
software floating-point, which costs exactly the speed that made the filtering
worth doing. The way out is to axiomatise \code{Float} against IEEE~754, as
Flocq does for Coq, so that the containment proofs become theorems relative to a
published standard rather than to bespoke code.

Extraction of a reasonably efficient executable has not been attempted. The
ordering on $A[\sqrt{n}]$ is computable by construction --- decidability of cone
membership is what makes it so --- but no attention has yet been paid to whether
the compiled code is fast, and some of the algebraic instances inherit
deliberately naive default implementations from Mathlib.

The rendering support is presently limited to producing static diagrams. The
interactive widget infrastructure available in Lean would permit dragging shapes
directly in the editor, which is the natural presentation for the drag-and-drop
application.

\chapter{References}

\begin{itemize}
\item Mark de Berg, Otfried Cheong, Marc van Kreveld and Mark Overmars,
  \emph{Computational Geometry: Algorithms and Applications}, 3rd edition,
  Springer, 2008. The primary source for the algorithms formalised here,
  including planar arrangements, the sweep-line method, point location and
  convex hulls.

\item The \link{https://www.cgal.org/}{CGAL} project, \emph{The Computational
  Geometry Algorithms Library}. Influential on the design of this library,
  particularly its separation of geometric algorithms from the arithmetic
  used to instantiate them, and its treatment of exact predicates.

\item Lutz Kettner, Kurt Mehlhorn, Sylvain Pion, Stefan Schirra and Chee Yap,
  \emph{Classroom examples of robustness problems in geometric computations},
  Computational Geometry \textbf{40}(1):61--78, 2008.
  \link{https://doi.org/10.1016/j.comgeo.2007.06.003}{doi:10.1016/j.comgeo.2007.06.003}.
  Exhibits concrete inputs on which floating-point implementations of convex
  hull and Delaunay triangulation fail, and analyses the geometry of the
  floating-point orientation predicate. This is the empirical case for
  Chapter~\ref{chap:numbers}.

\item The mathlib Community, \emph{The Lean Mathematical Library}, CPP 2020,
  pp.\ 367--381.
  \link{https://doi.org/10.1145/3372885.3373824}{doi:10.1145/3372885.3373824}.
  The source of the algebraic and order hierarchies used throughout, and of the
  \code{Set}-level definitions against which the geometric results are
  ultimately stated.

\item Leonardo de Moura and Sebastian Ullrich, \emph{The Lean 4 Theorem Prover
  and Programming Language}, CADE 28, LNCS 12699, pp.\ 625--635, Springer, 2021.
  \link{https://doi.org/10.1007/978-3-030-79876-5_37}{doi:10.1007/978-3-030-79876-5\_37}.
  Describes the system, including its treatment of Lean as a programming
  language, which is what makes it reasonable to expect these algorithms to run
  as well as to be proved correct.

\item Martin Rau and Tobias Nipkow, \emph{Verification of Closest Pair of Points
  Algorithms}, IJCAR 2020, LNCS 12167, pp.\ 341--357.
  \link{https://doi.org/10.1007/978-3-030-51054-1_20}{doi:10.1007/978-3-030-51054-1\_20}.
  The closest published analogue to this project: divide-and-conquer geometric
  algorithms verified for both functional correctness and asymptotic running
  time, with executable code extracted and benchmarked against handwritten
  implementations. A useful reference point for the choices discussed in
  Chapter~\ref{chap:arrangements}.

\item Tobias Nipkow, Manuel Eberl and Maximilian P.\ L.\ Haslbeck,
  \emph{Verified Textbook Algorithms: A Biased Survey}, ATVA 2020, LNCS 12302.
  \link{https://doi.org/10.1007/978-3-030-59152-6_2}{doi:10.1007/978-3-030-59152-6\_2}.
  Surveys what has been achieved in verifying standard algorithms, covering both
  correctness and running time, and is the most direct discussion of the
  trade-offs weighed in Chapter~\ref{chap:arrangements}: how much of a
  specification to state abstractly, when to refine a clean definition into an
  efficient implementation, and what is lost either way.

\item Adam Chlipala, \emph{Certified Programming with Dependent Types: A
  Pragmatic Introduction to the Coq Proof Assistant}, MIT Press, 2013.
  \link{https://adam.chlipala.net/cpdt/}{adam.chlipala.net/cpdt}.
  Written for Coq rather than Lean, but its subject is proof engineering rather
  than any particular prover: how to structure a development so that it survives
  change. Its treatment of dependently typed programming --- when to push
  correctness into the type of a function and when to keep it in a separate
  theorem --- is the question Chapter~\ref{chap:arrangements} has to answer for
  each algorithm.
\end{itemize}


% vim: ts=2 sw=2 et ai
